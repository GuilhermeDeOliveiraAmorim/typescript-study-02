{
  "name": "alurabank",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "lite-server --baseDir=app",
    "start": "node dist/src/index.js",
    "start:local": "concurrently \"npm run watch\" \"npm run server\"",
    "compile": "tsc",
    "watch": "tsc -w",
    "heroku-postbuild": "cd app && npm install && npm run build"
  },
  "engines" : { 
    "npm" : "8.3.1",
    "node" : "16.13.0"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^6.0.0",
    "lite-server": "^2.6.1",
    "typescript": "^4.2.2"
  }
}
